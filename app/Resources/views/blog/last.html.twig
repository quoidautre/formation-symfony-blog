<h4><a href="#" id="display-last-article" data-toggle="modal" data-target="#modal-last-article">Nos derniers articles...</a></h4>

<table class="table">
    {% for article in articles %}
        <tr>
            <td>
                <span class="label label-success"><a style="color:#fff" href="{{path('read_blog', {'id': article.id})}}">{{article.title}}</a></span>
            </td>
        </tr>
    {% endfor %}
</table>
